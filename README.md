# UnityHelpers
Some Helper functions and extensions I use.

To use Logger just do this.Log in a monobehaviour or ScriptableObject.
I like having class name in my debug messages so I made it default part of the message.
Made it easy by just making extensions of MonoBehavior and ScriptableObject.


Vector extensions just make it easier to convert vectors into other vectors without doing a new Vector(....).
